;; Enable Evil
(require 'evil)
(evil-mode 1)

;; Load a custom theme
(load-theme 'gruvbox t)

;; Disable menu bar
(menu-bar-mode -1)

;; Disable scrollbar
(toggle-scroll-bar -1)

;; Disable toolbar
(tool-bar-mode -1)
